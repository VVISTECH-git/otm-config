# Copy to .env for local dev. NEVER commit the real .env.
PORT=3001

# Local Postgres (Render provides DATABASE_URL automatically in prod)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/otmconfig

# OTM DBXMLServlet connection
OTM_BASE_URL=https://otmgtm-test-a629995.otmgtm.us-phoenix-1.ocs.oraclecloud.com
OTM_USER=TMS.TR_INT
OTM_PASS=change-me
OTM_DOMAIN=TMS
